import styled from "styled-components"

const StyledBlog = styled.div`
  height: 90vh;
  h1 {
    place-item: center;
    margin: auto;
    font-weight: normal;
    font-size: 4rem;
  }
`

export default StyledBlog
